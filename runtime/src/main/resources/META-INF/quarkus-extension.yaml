name: Authzed Client
description: A wrapper around the authzed client
metadata:
 keywords:
   - authzed-client
   - fga
# guide: ...
 categories:
   - authorization
   - security
 status: "preview"
 cli-plugins:
   - "${project.groupId}:quarkus-authzed-cli:runner:jar:${project.version}"
